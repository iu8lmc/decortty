// DecoRTTY — the small-caps label that titles every panel.
import QtQuick
import DecoRTTY

Text {
    font.pixelSize: 11
    font.letterSpacing: 1.4
    font.bold: true
    color: Theme.textSecondary
}
